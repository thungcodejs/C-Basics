#include<iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;
    if(a >= b){
        cout << "a is greater or equal than b";
    } else {
        cout << "a is smaller than b";
    }
    return 0;
}