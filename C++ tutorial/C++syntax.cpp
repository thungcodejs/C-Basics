#include<iostream>//là 1 thư viện tệp tiêu đè,bổ sung chức năng cho chương trình c++//
using namespace std;/*-sử dung không gian std,ngĩa là sủ dụng tên cho các đỗi tượng và biến từ thư viện chuẩn
-có thể bỏ qua ,thay vào là từ khóa std,cùng toán tử "::".Áp dụng cho 1 số đối tượng*/

int main() {
    cout << "Hello World!";//đối tượng xuất hiện cùng với toán tử "<<",để in/xuất văn bản//
    return 0;
}