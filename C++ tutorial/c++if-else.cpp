#include<iostream>

using namespace std;

int main() {
	int n;
	cin >> n;
	if (n % 2 == 0) {
		cout << "n is an even number";
	} else {
		cout << "n is an odd number";
	}
	return 0;
}
/* == là toán tử để so sánh giá trị có bằng nhau k
   = là toán tử để gán giá trị
   != là toán tử kiểm tra giá trị khác nhau không*/